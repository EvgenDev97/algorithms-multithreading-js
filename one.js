let amount = 0;
amout = 100;// variable
const  MAX_PURCHACE  = 2000 // global const UPPER_SNAKE_CASE
const total = 1500 //local const
console.log({total})

const calculate = (prices) =>{
    let amount = 0
    if(!Array.isArray(prices)) throw "Array expected"
    for (let item of prices){
        amount += prices
    }
    return amount
 }
 console.log(typeof  null)